const Constants = {
  eventTypes : ['Restaurants', 'Coffee', 'Bar', 'Movie', 'Sports', 'Casino', 'Others'],
  numbers : Array.apply(null, {length: 1000}).map(Number.call, Number)
}

export default Constants;
